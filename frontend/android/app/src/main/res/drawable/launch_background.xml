<?xml version="1.0" encoding="utf-8"?>
<!-- Professional branded launch screen: the Pdoczy mark, centered and calm,
     on the brand indigo. Shown on Android 11 and below (Android 12+ uses the
     system splash configured in values-v31/styles.xml). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/pdoczy_indigo" />
    <item
        android:width="176dp"
        android:height="176dp"
        android:gravity="center"
        android:drawable="@drawable/ic_launcher_foreground" />
</layer-list>
