name: Build Android APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.3'
          channel: stable
          cache: true

      - name: Setup Android project
        working-directory: frontend
        run: |
          flutter create --project-name ai_pdf --org com.aidocassistant --platforms android .
          git checkout -- lib/ pubspec.yaml analysis_options.yaml || true
          git checkout -- android/app/src/main/AndroidManifest.xml || true
          git checkout -- android/app/build.gradle || true
          git checkout -- android/build.gradle || true
          git checkout -- android/settings.gradle || true
          git checkout -- android/gradle.properties || true
          git checkout -- android/gradle/wrapper/gradle-wrapper.properties || true
          git checkout -- android/app/src/main/kotlin/ || true
          git checkout -- android/app/src/main/res/ || true

      - name: Install dependencies
        working-directory: frontend
        run: flutter pub get

      - name: Check for errors
        working-directory: frontend
        run: flutter analyze --no-fatal-infos --no-fatal-warnings || true

      - name: Build release APK
        working-directory: frontend
        run: flutter build apk --release

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: AI-PDF-Release
          path: frontend/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
